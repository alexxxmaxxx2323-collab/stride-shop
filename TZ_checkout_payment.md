# ТЗ: Lamoda-чекаут, авторегистрация, ЮKassa, UI-правки

> Версия от 2026-05-30. Источник — разбор живого чекаута Lamoda (`https://www.lamoda.ru/checkout/cart/`) + требования заказчика.
> Принцип: **по функционалу как у Lamoda, по вёрстке/расположению — в нашем стиле**.

---

## ✅ ПРОГРЕСС (обновлено 2026-05-31)

> Ветка `design-test`. Последний коммит сессии 31.05: **`d72abda`** (запушен в origin).
> Сервер локально: `cd backend && .venv\Scripts\python.exe -m uvicorn app.main:app --port 8077`
> → витрина `http://127.0.0.1:8077/static/shop.html`. У страницы `<meta no-store>`; при правках всё равно Ctrl+F5.
> Тестировать в ОБЫЧНОМ браузере, НЕ в окне Playwright (перехват файлов/кликов).

### Сделано и проверено вживую (авторегистрация — сессии до 31.05)
- **Бэкенд авторегистрации.** Модель `User` + поля `phone`, `is_guest`, `email_verified`, `email_verify_token`, `marketing_consent` (миграция `d1e2f3a4b5c6`). Эндпоинты: `POST /auth/guest`, `POST /auth/checkout-register`, `GET /auth/verify-email?token=`. `POST /auth/register` тоже шлёт письмо-подтверждение.
- **E-mail.** `app/services/email.py` — SMTP + demo-заглушка (`[email:STUB]` в консоль). Конфиг `SMTP_*`, `YOOKASSA_*` в `app/config.py`.
- **Фронт-чекаут (Lamoda) на тёмной витрине.** Блок «Кто заберёт заказ?» (Имя/Фамилия/Телефон/Email), один маркетинг-чекбокс, click-wrap. Гость без логина: `/auth/guest` → `/auth/checkout-register` → `mergeGuestData` → заказ → `payment_url=/static/payment.html?order=N`.
- **Промо-плашка «−5%»** в корзине/избранном. Smoke checkout/cart/orders/admin — зелёные.

### Сделано в СЕССИИ 31.05 (всё в коммите d72abda)
- **UI-правки карточек:** название модели — лаймом `#ccff00`, цена белой, рейтинг белый (единая палитра: 1 акцент-лайм + 1 сигнал-красный, без золота — по требованию юзера «не пестрить»).
- **Цвет-кружки:** убраны из карточек списка; в модалке — САМА кнопка `.pm-color` залита `style="background:<hex>"` напрямую (старый вложенный `.pm-color-inner` выпилен — у него «не доезжала» заливка), контур светло-серый, у выбранного — лаймовый. (`renderProductModal` в shop.html.)
- **Реальные расцветки + фото на каждый цвет.** Скрипт `backend/_real_colorways.py` (идемпотентный): зондирует StockX-360, качает подтверждённые папки в `static/products/<slug>/c<idx>/01..05.jpg`, в БД сносит фейки и создаёт реальные варианты. Итог: 11 моделей по 2 цвета (своё фото на цвет — при клике фото меняется), остальные по 1 реальному. **NB:** данные вариантов в `shop.db` (в .gitignore) — после пересева базы прогнать `python _real_colorways.py` заново.
- **Скраб фото по наведению** на карточку каталога (mousemove листает кадры, точки-индикатор) — `setupScrub()` в shop.html.
- **Тег Hot** с пояснением (title «хит продаж»); плашка «Быстрый выбор» убрана.
- **Фикс скролла аккордеона** в модалке: клик по «О товаре/Состав/...» перерисовывает ТОЛЬКО `#pmAcc` (`cur.outerHTML = renderAccordion(p)`), а не всю модалку — окно больше не прыгает вверх.
- **Видимость ошибок чекаута:** функция `showCoError()` — toast по центру + подскролл к `#coError`. Раньше ошибка пряталась внизу длинной формы → казалось «не пускает». (Бэкенд-флоу заказа и геокодер проверены — рабочие; баг был только в невидимости ошибки.)
- **Логотипы оплаты:** в `static/payment.html` пиксельные эмодзи заменены на векторные `static/img/{sbp,mir,visa,mastercard}.svg` (собственные SVG-отрисовки под демо до подключения ЮKassa).

### Сделано в СЕССИИ 31.05 (вечер) — Личный кабинет + подтверждение e-mail (НЕ закоммичено)
- **Бэк, 2 новые ручки в `app/routers/auth.py`:**
  - `POST /auth/resend-verification` — повторная отправка письма подтверждения (auth; 400 если уже подтверждён или нет e-mail; генерит новый `email_verify_token`, шлёт письмо фоном).
  - `PATCH /auth/me` — partial-update профиля (`first_name`/`last_name`/`phone`/`marketing_consent`) через `model_dump(exclude_unset=True)`; e-mail здесь не меняем. Новая схема `ProfileUpdateIn` в `schemas/auth.py` (телефон нормализуется тем же `normalize_phone`).
- **Фронт `static/shop.html` — панель «Личный кабинет»** (`#profilePanel`/`#profileBody`): клик по имени в шапке (`#userLabel`) открывает ЛК. Внутри: приветствие+e-mail, **блок статуса подтверждения** (зелёный «✓ подтверждён» / красный «⚠ не подтверждён» + кнопка «Выслать письмо повторно» → `resendVerification()`), форма редактирования (имя/фамилия/телефон/маркетинг-чекбокс) → `saveProfile()` (PATCH), ссылка «Мои заказы» → открывает существующую панель заказов. JS-функции `openProfile/renderProfile/saveProfile/resendVerification/openProfileOrders`; CSS-класс `.pf-*` в тёмной палитре. `closeCart()` закрывает и панель ЛК.
- **Проверено:** curl (PATCH нормализует телефон `+79161234567`, resend пишет `[email:STUB]`, плохой телефон → 422 с понятным текстом); `node --check` инлайн-JS — чисто; smoke `smoke_checkout_auth` + `smoke_cart_orders` + `smoke_admin` — зелёные (cart_orders требовал очистки остатка корзины alice — изоляция теста, не баг). Браузерную проверку делает юзер на `http://127.0.0.1:8077/static/shop.html` (Ctrl+F5).

### Сделано в СЕССИИ 31.05 (ночь) — Полноценная страница ЛК (НЕ закоммичено)
Решения юзера: Gmail SMTP подключаем (ждём App Password), все 4 доп.раздела, бонусы/офферы = рабочая логика.
- **Миграция `e1f2a3b4c5d6`** (от d1e2f3a4b5c6, применена локально + бэкфилл реф-кодов 31 юзеру): новые модели `Address` (адресная книга), `BonusTransaction` (леджер бонусов); поля User: `sms_consent`, `preferred_size`, `referral_code`(unique), `referred_by`.
- **Сервис `app/services/bonus.py`:** правила (CASHBACK_PCT=5, REFERRER_BONUS=500, REFERRED_BONUS=300), `bonus_balance` (= сумма леджера), `credit_bonus`, `ensure_referral_code`, `apply_referral`. Кэшбэк 5% начисляется в `create_order`. Реферал — в `register`/`checkout-register` по полю `ref`.
- **Роутер `app/routers/account.py` (prefix `/me`):** `GET /me/summary` (заказы/потрачено/баланс), `/me/bonuses` (баланс+история), `/me/referral` (код+ссылка+приглашено+заработано), `/me/offers` (товары со скидкой, в приоритете бренды из избранного), CRUD `/me/addresses` (первый адрес→дефолт, переезд дефолта при удалении). Схемы — `app/schemas/account.py`.
- **Auth:** `POST /auth/change-password` (проверка текущего); `ProfileUpdateIn` расширен `sms_consent`+`preferred_size` (валидатор 30–50); `UserOut` отдаёт `sms_consent`/`preferred_size`/`referral_code`; register принимает `ref`. Грабли: с `from __future__ import annotations` FastAPI трактует `-> None` как `NoneType`-тело → конфликт с 204 (убрал аннотацию на delete).
- **Фронт:** новая страница **`static/account.html`** (sidebar, 9 разделов: Обзор/Заказы/Бонусы/Предложения/Избранное+мой размер/Адреса/Уведомления/Пригласить друга/Профиль+смена пароля; hash-роутер; тёмная премиум-тема). В `shop.html` — **иконка профиля** (человечек) в шапке + клик по имени → `account.html`; старая боковая панель ЛК больше не вызывается; добавлен захват `?ref=CODE` (localStorage `stride_ref`) и проброс в обе регистрации.
- **Проверено:** smoke `tests/smoke_account.py` (кэшбэк +599 за заказ 11988, рефералка friend+300/alice+500, адреса default-логика, размер/SMS, смена пароля) + регрессы checkout_auth/cart_orders/admin — все зелёные; `node --check` обоих html чисто; страницы отдаются 200. Сервер на 8077 поднят для браузерной проверки.

### Правки ЛК по фидбеку (СЕССИЯ 31.05, поздняя — НЕ закоммичено)
- **Баг «нет баллов при оплаченной покупке»** — кэшбэк начислялся только новым заказам; старые имели 0. Фикс: идемпотентный скрипт `backend/_backfill_cashback.py` (за каждый заказ без бонус-tx начисляет 5%). Прогнан: 11 заказов начислено, юзер id=28 → 3172, alice → 3220. NB: кэшбэк сейчас начисляется при СОЗДАНИИ заказа (pending), не при оплате.
- **Баг «реф-ссылка не работает»** — `/me/referral.link` строился от `site_url=:8000` (конфиг по умолч.), а магазин на `:8077` → на 8000 крутится TG-Auto → «Not Found». Фикс: фронт `account.html` строит ссылку от `location.origin` (бэкенд-link оставлен как fallback для прода).
- **Навигация ЛК** — убраны эмодзи, поставлены line-SVG иконки (Feather-стиль, accent на активном); меню прижато к левому краю и выше (wrap 1320px, sidebar 208px, `.nav top:78px`, убран большой заголовок-блок).
- **Честность формулировок:** убраны обещания «адрес подставляется в чекаут» и «подскажем наличие по размеру» (пока не интегрировано).
- **ДИАГНОСТИРОВАННЫЕ ЗАЗОРЫ взаимосвязей (НЕ баги, а недоделанные интеграции, кандидаты в работу):**
  1. **Адресная книга ↔ чекаут НЕ связаны** — сохранённые адреса не подставляются в форму оформления shop.html (там свой инпут). Главный кандидат на доводку.
  2. **Бонусы нельзя потратить** — только начисление; «оплата баллами» на чекауте не сделана.
  3. **preferred_size не используется** для фильтра «есть в моём размере» в каталоге.
  4. **Тумблеры уведомлений** сохраняются, но рассылки (marketing email/SMS) не реализованы; письмо-подтверждение шлётся всегда.
  5. Форма смены пароля показывается и TG/гость-аккаунтам без пароля (бэк отвечает 400 с понятным текстом — обрабатывается, но можно скрывать).

### Логика раздела «Предложения» (offers) — как подбирается
`GET /me/offers`: берём товары со скидкой (`price_old > price`); **в приоритете бренды, которые юзер добавлял в избранное** (join Favorite→Product→brand_id); добор до 8 — топ-рейтинговыми скидочными. reason = «На основе ваших избранных брендов» либо «Лучшие предложения со скидкой». История заказов в подбор пока НЕ входит (кандидат на улучшение).

### Закрыты 3 зазора взаимосвязей (СЕССИЯ 31.05, поздняя — коммит после 8553b07)
- **Адреса → чекаут:** `renderCheckout` подтягивает `/me/addresses` (быстрый выбор-чипы заполняют имя/телефон/адрес, переключают на курьера) + чекбокс «Сохранить адрес в кабинете» (POST `/me/addresses` best-effort после заказа). Функции `loadCheckoutExtras/renderSavedAddresses/applySavedAddress`.
- **Оплата баллами:** `OrderCreate.use_points`; в `create_order` списываем `min(use_points, баланс, сумма)`, total = нетто, отрицательная BonusTransaction «Оплата баллами», кэшбэк считается с НЕТТО. Фронт: блок `#coPoints` (чекбокс + input, live-пересчёт «К оплате»), `coBalance/coUsePoints`. Проверено: заказ нетто=gross−1000, −1000 списание, +кэшбэк с нетто, баланс сходится.
- **Фильтр «мой размер»:** бэкенд `/products?size=` УЖЕ был; добавлен чип `#mySizeChip` в тулбар каталога (виден если есть `preferred_size`), тогл добавляет `&size=` в loadCatalog. Проверено: size=47→0, 43→26.

### Фиксы по фидбеку №2 (СЕССИЯ 31.05, поздняя — НЕ закоммичено пока)
- **Кэшбэк только после оплаты:** убран из `create_order`; начисляется в `payments.mock_pay` при `payment_status='paid'` (guard от двойного, `_credit_cashback_once`). Баллы-списание остаётся при создании (резерв от двойной траты). Скрипт `_backfill_cashback.py` переписан в СВЕРКУ: снимает кэшбэк с неоплаченных, оставляет оплаченным (прогнан: снято 20, у paid #7/#14 сохранён). Проверено: заказ→balance 0, оплата→+кэшбэк.
- **Строгая валидация адреса в адресной книге:** вынесены `validate_address_string`/`normalize_ru_phone` в `schemas/order.py`, применены в `AddressIn` (full_address: запятые/цифра/антибред; phone: +7 11 цифр); геокодер `_verify_real_address` в create/update_address (как на чекауте). Фронт account.html: маска телефона. Проверено: гиббериш→422, плохой тел→422, реальный кириллический адрес→201.
- **Алгоритм «Предложения» переписан на скоринг:** сигнал = бренды+категории из избранного И покупок; скор: +3 бренд, +2 категория, +2 скидка, +rating·0.5; знакомые товары исключаются; без сигнала → «популярное со скидкой». reason отражает источник.

### Жизненный цикл заказа (СЕССИЯ 31.05, поздняя — НЕ закоммичено пока)
Реализован полноценный lifecycle заказа по образцу РФ-магазинов (WB/Ozon/Lamoda). **Две независимые оси** (как у Ozon): `payment_status` (оплата) и `status` (исполнение).
- **Машина состояний** `app/services/order_status.py`: статусы pending→assembling→shipped→[курьер: in_transit | ПВЗ: ready_for_pickup]→delivered, ветки cancelled/delivery_failed/returned; RU-метки STATUS_LABELS/PAYMENT_LABELS; `allowed_transitions(status, delivery_type)`, `can_transition`. CUSTOMER_CANCELLABLE={pending,assembling}, REVERSAL_STATUSES={cancelled,returned}.
- **Развязка осей:** `payments.mock_pay` теперь меняет ТОЛЬКО payment_status (paid/cod), не трогает исполнение. Старые заказы со status='paid' пофикшены в pending (инлайн-скрипт).
- **Отмена клиентом** `POST /orders/{id}/cancel` (только pending/assembling); `reverse_order_bonuses` (services/bonus.py) — одной компенсирующей операцией откатывает списанные баллы + снимает кэшбэк; payment_status→refunded если был paid.
- **Админка заказов:** `GET /admin/orders` (+ allowed_next/метки), `PATCH /admin/orders/{id}/status` (валидация перехода через машину; при cancelled/returned — откат бонусов+refund). Схема `AdminOrderOut`. Вкладка «Заказы» в admin.html (таблица + кнопки смены статуса под ветку доставки).
- **Фронт ЛК:** полные RU-метки исполнения + бейдж оплаты (две оси), кнопка «Отменить заказ» когда можно.
- **Тест:** `tests/smoke_order_lifecycle.py` — оплата не трогает исполнение, ПВЗ/курьер ветки, невалидный переход→409, отмена+возврат баллов, отмена после отгрузки→409, админ-отмена оплаченного (откат кэшбэка+refunded). Все 5 smoke-наборов зелёные. Регрессий нет (payment.html/мини-аппа смотрят payment_status, не status).
- **NB на будущее:** delivery_failed/returned есть в машине; уведомления о смене статуса (письмо/TG) не шлются — кандидат.

### Осталось (по этому ТЗ) — НАЧАТЬ ОТСЮДА
0. **Браузерная проверка ЛК** юзером на `http://127.0.0.1:8077/static/account.html` (открыть в ОБЫЧНОМ браузере, залогиниться, пройти разделы). **Gmail SMTP:** юзер создаёт App Password → креды в `backend/.env` (SMTP_HOST=smtp.gmail.com, PORT=587, TLS=true, USER/PASSWORD/FROM) → письма реально пойдут (каркас готов).
1. **ЮKassa (sandbox) — главный следующий пункт. НЕ начато.** Реальный виджет `yookassa-checkout-widget` в ТЕСТ-режиме (СБП/карта МИР/кошельки, тест-карты, деньги не уходят). Бэк: создание платежа → `confirmation_token`, webhook `payment.succeeded`→paid + fallback на локалке. Заменить самодельный `/payments/mock` UI. **Ждём от юзера:** регистрация на yookassa.ru → тест `YOOKASSA_SHOP_ID` + `YOOKASSA_SECRET_KEY` в `backend/.env`. Пакет `yookassa` ставить с разрешения. Подробности — раздел 5 ниже. Текущие SVG-логотипы оплаты тогда заменит родной виджет.
2. **SMS** — провайдер не выбран; на демо заглушка «отправлено».
3. **Реальный SMTP** — не подключён (сейчас demo-лог `[email:STUB]`). Каркас писем готов (`app/services/email.py`) — нужны только креды в `.env`.
4. ~~**Подтверждение e-mail / ЛК**~~ — ✅ СДЕЛАНО (сессия 31.05 вечер, см. выше). Открытый хвостик: смена e-mail из ЛК с повторным подтверждением (сейчас e-mail в профиле не редактируется).
5. Открытый вопрос: мини-окно регистрации по телефону при «Купить» — под вопросом.

### Скрипты-утилиты (репо, воспроизводимые)
- `backend/_real_colorways.py` — расцветки + пересборка вариантов (см. выше).
- `backend/_download_photos.py`, `_add_neon.py` — прежние загрузчики фото.
- Тестовый юзер: `alice@test.com` / `hunter22pass` (is_admin). Товар «Test model 1» (id 25, slug `test-model-1`) — юзерский, не трогать.

---

## 1. Гостевой режим + авторегистрация на чекауте

**Как у Lamoda: единая страница оформления, БЕЗ всплывающих окон.**

- Пользователь без регистрации: смотрит каталог, добавляет в корзину и избранное, выбирает размер/цвет, начинает оформление.
- Текущую стандартную форму регистрации, которая открывается при заказе, **убрать**.
- На странице оформления — блок **«Кто заберёт заказ?»** прямо inline:
  - **Имя\*** · **Фамилия\***
  - **Телефон\*** · **Email\***
- При нажатии «Оформить заказ» аккаунт **создаётся автоматически** по этим данным (отдельная регистрация не нужна).
- Вверху страницы — мягкий баннер-подсказка «Авторизуйтесь, чтобы видеть персональные условия и отслеживать заказы» (не принуждение).

### Согласия (решение: click-wrap, как у Lamoda)
- **Один** чекбокс: ☑ «Получать уведомления о заказах и скидках» — **стоит по умолчанию** (маркетинг).
- Согласие с офертой/обработкой ПДн — **текстом под кнопкой**: «Нажимая "Оформить заказ", вы соглашаетесь с офертой» + кликабельная ссылка на документ (открывает текст). Без обязательной галочки.

---

## 2. Подтверждения и личный кабинет

- **SMS** — после оформления заказа на указанный телефон шлём СМС для подтверждения заказа (как у Lamoda: «После оформления заказа пришлём СМС для подтверждения»).
- **Email** — после авторегистрации на почту приходит письмо «подтвердите адрес».
- **Личный кабинет** создаётся автоматически после заказа; в нём — вся история заказов и покупок.

---

## 3. Второй путь регистрации + промо

- Обычная регистрация по почте через кнопку «Войти» (уже есть в шапке) — оставляем.
- Телефон в заказе берём **всегда**, независимо от способа.
- **Плашка «Зарегистрируйтесь — −5%»** в корзине и в избранном; клик → форма регистрации.
  (У Lamoda аналог: «Скидка 10% на первые 3 заказа после регистрации» + «Войти».)

### Под вопросом (решить позже)
- Мини-окно регистрации по номеру телефона при клике «Купить» — заказчик сам не уверен, нужно ли. Помечено как кандидат.

---

## 4. Опционально из Lamoda (по желанию заказчика)

- Выбор **даты и слота доставки** прямо в чекауте (чипы дат + слоты времени).
- Переключатель оплаты **«При получении / Сейчас»** на том же экране.

---

## 5. Оплата — ЮKassa (тестовый магазин / sandbox)

**Решение: реальная интеграция ЮKassa в ТЕСТ-режиме («реальная ЮKassa с заглушкой»).**

- На бэке создаём платёж через API ЮKassa → получаем `confirmation_token`.
- На фронте рендерим **официальный виджет ЮKassa** (`yookassa-checkout-widget`): настоящий вид со всеми способами — СБП, банковская карта (МИР/Visa/MC) с реальными полями ввода, кошельки.
- Оплата тестовыми картами; **реальные деньги не списываются** (это и есть «заглушка в конце»).
- Webhook `payment.succeeded` → заказу ставим `payment_status=paid`. На локалке (без публичного webhook) — короткий fallback, помечающий заказ оплаченным.
- Старую заглушку `/payments/mock` и самодельный выбор СБП/МИР/cod — **заменяем** на виджет ЮKassa.

### Нужно от заказчика
1. Зарегистрировать магазин на **yookassa.ru** (бесплатно).
2. Включить **тестовый магазин** → получить `shopId` + секретный ключ (test).
3. Передать их → пропишем в `backend/.env`: `YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY`.

### Зависимости
- Пакет `yookassa` в `backend/.venv` (поставить с разрешения — см. CLAUDE.md).

---

## 6. UI-правки витрины и карточек

- **Рейтинг товара** (напр. 4.7): звёзды **золотистые, пожирнее**, сам рейтинг заметнее (сейчас сливается).
- **Карточки товара**: цена и название белым сливаются с фоном — **цену выделить контрастным цветом**, название сделать различимым.

---

## Открытые вопросы / зависимости от провайдеров

| Тема | Статус | Что нужно решить |
|---|---|---|
| SMS-провайдер | НЕ выбран | SMS.ru / Twilio / провайдер ЮKassa? Нужен аккаунт+API-ключ. На время демо — можно лог/заглушку «отправлено». |
| Email (SMTP) | НЕ выбран | Gmail SMTP / Mailgun / SendGrid? Нужны креды. На время демо — лог/заглушка. |
| ЮKassa test-креды | ЖДЁМ от заказчика | shopId + secret (test) |
| Мини-окно рег. по телефону | под вопросом | делать или нет |

---

## Порядок реализации (черновик, уточнить)
1. Бэк: модель/флоу авторегистрации на чекауте (гость → user по имени/фамилии/тел/почте).
2. Фронт чекаута: Lamoda-блок «Кто заберёт заказ?» + click-wrap + маркетинг-чекбокс; убрать старую форму.
3. Email-подтверждение адреса + ЛК с историей.
4. SMS-подтверждение заказа (с заглушкой, если провайдер не выбран).
5. Плашка «−5%» в корзине/избранном.
6. ЮKassa sandbox: бэк (создание платежа, webhook) + фронт-виджет; убрать /payments/mock UI.
7. UI: рейтинг (золотые звёзды) + контраст цены/названия в карточках.
8. (опц.) дата/слот доставки, переключатель оплаты.
